<Window
    x:Class="TreeViewCrud.Views.UsersManagementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Управление пользователями"
    Width="700"
    Height="500"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button
            Width="150"
            Height="30"
            Margin="5"
            Click="AddUser_Click"
            Content="➕ Добавить пользователя" />

        <ListView
            x:Name="lvUsers"
            Grid.Row="1"
            Margin="5"
            SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="50"
                        DisplayMemberBinding="{Binding UserId}"
                        Header="ID" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding Login}"
                        Header="Логин" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding LastName}"
                        Header="Фамилия" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding FirstName}"
                        Header="Имя" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding Role}"
                        Header="Роль" />
                    <GridViewColumn Width="60" Header="Активен">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel
            Grid.Row="2"
            Margin="5"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="100"
                Height="30"
                Margin="5"
                Click="EditUser_Click"
                Content="✏️ Редактировать" />
            <Button
                Width="100"
                Height="30"
                Margin="5"
                Click="DeleteUser_Click"
                Content="❌ Удалить" />
            <Button
                Width="100"
                Height="30"
                Margin="5"
                Click="Close_Click"
                Content="Закрыть" />
        </StackPanel>
    </Grid>
</Window>